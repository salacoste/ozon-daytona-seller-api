## Section 43 — Beta Group: Quants (Part 1 of 1)

Scope: Implement beta `05-quants.json` (2 endpoints: квантовая упаковка).

Endpoints:
1) POST `/v1/product/quant/list`
2) POST `/v1/product/quant/info`

SDK namespace: `client.quants` → `listV1`, `infoV1`.

Tasks: types (`src/types/quants.beta.part1.ts`), API (`src/apis/quants.beta.ts`), tests, docs, examples; mark beta.

---
