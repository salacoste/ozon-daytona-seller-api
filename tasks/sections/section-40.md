## Section 40 — P0 Group: ReturnsAPI (Part 1 of 1)

Scope: Implement `methods/27-returnsapi.json` (1 endpoint).

Endpoint:
1) POST `/v1/returns/list`

SDK namespace: `client.returns` → `listV1`.

Tasks: types (`src/types/returns.part1.ts`), API (`src/apis/returns.ts`), tests, docs, examples, coverage ≥ 80%.

---
