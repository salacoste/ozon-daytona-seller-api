---
description: Валидация и ZOD схемы в слое SDK
alwaysApply: false
---
Схемы и типы (валидация контрактов)

Схемы запросов/ответов генерируем из ozon-api-docs (через MCP) → приводим к Zod схемам и TS типов.

Любой ответ парсим Zod-схемой в слое SDK-метода. Не пропускаем невалидный контракт.
Пагинация, курсоры, массовые выгрузки

Вынести в src/utils/paginate.ts генераторы async *paginate(...).

Запрещено писать ручные циклы в категориях — только общий helper.

 