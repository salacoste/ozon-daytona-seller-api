# 22.2 API Таймеры актуальности цен

**API Таймеры актуальности цен** - Управление таймерами актуальности минимальных цен для OZON Seller API.

## Обзор

API Таймеры актуальности предоставляет управление автоматическими таймерами, которые контролируют актуальность минимальных цен товаров. Эта система помогает автоматически поддерживать конкурентоспособные цены и соответствовать требованиям платформы OZON.

### Ключевые особенности
- **Автоматический контроль цен**: Таймеры следят за актуальностью минимальных цен
- **Гибкое управление**: Включение и выключение таймеров для отдельных товаров
- **Массовые операции**: Управление таймерами для множества товаров одновременно
- **Статус-мониторинг**: Проверка состояния таймеров для товаров

---

## 📋 Доступные методы

### Управление таймерами
1. **getActionTimerStatus** - Получить статус таймеров для товаров
2. **updateActionTimer** - Обновить/активировать таймеры для товаров

---

## 🛠️ Детали методов

## 1. getActionTimerStatus

Получение статуса установленных таймеров актуальности минимальной цены.

### Интерфейс запроса
```typescript
interface PricesStocksActionTimerStatusRequest {
  product_ids: string[];          // Массив ID товаров для проверки
}
```

### Интерфейс ответа
```typescript
interface PricesStocksActionTimerStatusResponse {
  statuses?: Array<{
    product_id: string;           // ID товара
    is_timer_enabled: boolean;    // Включён ли таймер
    timer_expires_at?: string;    // Дата истечения таймера (ISO)
    last_price_update?: string;   // Последнее обновление цены (ISO)
    min_price?: string;           // Минимальная цена товара
    current_price?: string;       // Текущая цена товара
    recommended_price?: string;   // Рекомендованная цена
  }>;
}
```

### Примеры использования

#### Проверка статуса таймеров
```typescript
async function проверитьСтатусТаймеров() {
  const статус = await api.pricesStocks.getActionTimerStatus({
    product_ids: ['123456', '234567', '345678', '456789']
  });
  
  console.log('⏰ Статус таймеров актуальности цен:');
  
  if (!статус.statuses || статус.statuses.length === 0) {
    console.log('🔍 Информация о таймерах не найдена');
    return;
  }
  
  статус.statuses.forEach(товар => {
    console.log(`\n📦 Товар ID: ${товар.product_id}`);
    console.log(`   ⏰ Таймер: ${товар.is_timer_enabled ? '✅ включён' : '❌ выключен'}`);
    
    if (товар.timer_expires_at) {
      const истекает = new Date(товар.timer_expires_at);
      const сейчас = new Date();
      const часовДоИстечения = Math.round((истекает.getTime() - сейчас.getTime()) / (1000 * 60 * 60));
      
      console.log(`   📅 Истекает: ${истекает.toLocaleString()}`);
      
      if (часовДоИстечения <= 24) {
        console.log(`   ⚠️ Внимание: таймер истекает через ${часовДоИстечения} часов`);
      } else {
        console.log(`   📊 До истечения: ${часовДоИстечения} часов`);
      }
    }
    
    if (товар.min_price) {
      console.log(`   💰 Минимальная цена: ${товар.min_price} RUB`);
    }
    
    if (товар.current_price) {
      console.log(`   💵 Текущая цена: ${товар.current_price} RUB`);
    }
    
    if (товар.recommended_price) {
      console.log(`   💡 Рекомендованная цена: ${товар.recommended_price} RUB`);
      
      // Анализ соответствия рекомендации
      if (товар.current_price && товар.recommended_price) {
        const текущая = parseFloat(товар.current_price);
        const рекомендованная = parseFloat(товар.recommended_price);
        const разница = Math.abs(текущая - рекомендованная) / рекомендованная * 100;
        
        if (разница > 10) {
          console.log(`   📈 Рекомендация: ${текущая > рекомендованная ? 'снизить' : 'повысить'} цену на ${разница.toFixed(1)}%`);
        } else {
          console.log(`   ✅ Цена соответствует рекомендациям`);
        }
      }
    }
    
    if (товар.last_price_update) {
      const последнееОбновление = new Date(товар.last_price_update);
      const часовСОбновления = Math.round((Date.now() - последнееОбновление.getTime()) / (1000 * 60 * 60));
      console.log(`   🔄 Последнее обновление цены: ${часовСОбновления} часов назад`);
    }
  });
  
  return статус;
}
```

#### Анализ эффективности таймеров
```typescript
async function анализЭффективностиТаймеров() {
  // Получаем статус для большого количества товаров (пакетами)
  const всеТовары = ['123456', '234567', '345678', '456789', '567890']; // В реальности больше товаров
  
  const результаты = [];
  const размерПакета = 100; // API может ограничивать количество
  
  for (let i = 0; i < всеТовары.length; i += размерПакета) {
    const пакет = всеТовары.slice(i, i + размерПакета);
    
    try {
      const статус = await api.pricesStocks.getActionTimerStatus({
        product_ids: пакет
      });
      
      if (статус.statuses) {
        результаты.push(...статус.statuses);
      }
      
      // Пауза между запросами
      if (i + размерПакета < всеТовары.length) {
        await new Promise(resolve => setTimeout(resolve, 1000));
      }
      
    } catch (error: any) {
      console.error(`❌ Ошибка получения статуса для пакета ${i}-${i + размерПакета}: ${error.message}`);
    }
  }
  
  // Анализируем результаты
  const анализ = {
    общееКоличество: результаты.length,
    включённыеТаймеры: результаты.filter(т => т.is_timer_enabled).length,
    истекающиеТаймеры: результаты.filter(т => {
      if (!т.timer_expires_at) return false;
      const часовДоИстечения = (new Date(т.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60);
      return часовДоИстечения <= 24;
    }).length,
    требуютОбновления: результаты.filter(т => {
      if (!т.current_price || !т.recommended_price) return false;
      const разница = Math.abs(parseFloat(т.current_price) - parseFloat(т.recommended_price)) / parseFloat(т.recommended_price);
      return разница > 0.1; // Больше 10% разницы
    }).length,
    старыеЦены: результаты.filter(т => {
      if (!т.last_price_update) return false;
      const часовСОбновления = (Date.now() - new Date(т.last_price_update).getTime()) / (1000 * 60 * 60);
      return часовСОбновления > 168; // Больше недели
    }).length
  };
  
  console.log('📊 Анализ эффективности таймеров:');
  console.log(`   📦 Всего товаров: ${анализ.общееКоличество}`);
  console.log(`   ⏰ Включённые таймеры: ${анализ.включённыеТаймеры} (${Math.round(анализ.включённыеТаймеры / анализ.общееКоличество * 100)}%)`);
  console.log(`   ⚠️ Истекают в 24ч: ${анализ.истекающиеТаймеры}`);
  console.log(`   💰 Требуют коррекции цены: ${анализ.требуютОбновления}`);
  console.log(`   🕰️ Старые цены (>7 дней): ${анализ.старыеЦены}`);
  
  // Рекомендации
  if (анализ.истекающиеТаймеры > 0) {
    console.log('\n💡 Рекомендации:');
    console.log(`   🔄 Обновите таймеры для ${анализ.истекающиеТаймеры} товаров`);
  }
  
  if (анализ.требуютОбновления > 0) {
    console.log(`   💰 Скорректируйте цены для ${анализ.требуютОбновления} товаров`);
  }
  
  if (анализ.старыеЦены > 0) {
    console.log(`   📈 Проанализируйте актуальность цен для ${анализ.старыеЦены} товаров`);
  }
  
  return анализ;
}
```

#### Мониторинг критических таймеров
```typescript
async function мониторингКритическихТаймеров() {
  const критическиеТовары = ['123456', '234567', '345678']; // Важные товары
  
  const статус = await api.pricesStocks.getActionTimerStatus({
    product_ids: критическиеТовары
  });
  
  if (!статус.statuses) return;
  
  const критическиеСитуации = [];
  
  статус.statuses.forEach(товар => {
    const ситуации = [];
    
    // Проверяем таймер
    if (!товар.is_timer_enabled) {
      ситуации.push({
        тип: 'отключённый_таймер',
        описание: 'Таймер актуальности отключён',
        приоритет: 'высокий'
      });
    }
    
    // Проверяем истечение
    if (товар.timer_expires_at) {
      const часовДоИстечения = (new Date(товар.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60);
      
      if (часовДоИстечения <= 2) {
        ситуации.push({
          тип: 'скорое_истечение',
          описание: `Таймер истекает через ${Math.round(часовДоИстечения)} часов`,
          приоритет: 'критический'
        });
      } else if (часовДоИстечения <= 12) {
        ситуации.push({
          тип: 'предупреждение',
          описание: `Таймер истекает через ${Math.round(часовДоИстечения)} часов`,
          приоритет: 'средний'
        });
      }
    }
    
    // Проверяем соответствие рекомендациям
    if (товар.current_price && товар.recommended_price) {
      const текущая = parseFloat(товар.current_price);
      const рекомендованная = parseFloat(товар.recommended_price);
      const разницаПроцентов = Math.abs(текущая - рекомендованная) / рекомендованная * 100;
      
      if (разницаПроцентов > 20) {
        ситуации.push({
          тип: 'несоответствие_цены',
          описание: `Цена отклоняется от рекомендации на ${разницаПроцентов.toFixed(1)}%`,
          приоритет: 'высокий'
        });
      }
    }
    
    if (ситуации.length > 0) {
      критическиеСитуации.push({
        товарId: товар.product_id,
        ситуации
      });
    }
  });
  
  if (критическиеСитуации.length === 0) {
    console.log('✅ Критические ситуации с таймерами не обнаружены');
    return;
  }
  
  console.log('🚨 Обнаружены критические ситуации с таймерами:');
  
  критическиеСитуации.forEach(товар => {
    console.log(`\n📦 Товар ${товар.товарId}:`);
    
    товар.ситуации.forEach(ситуация => {
      const эмодзи = ситуация.приоритет === 'критический' ? '🔴' : 
                    ситуация.приоритет === 'высокий' ? '🟠' : '🟡';
      
      console.log(`   ${эмодзи} ${ситуация.описание} (${ситуация.приоритет})`);
      
      // Рекомендации по устранению
      switch (ситуация.тип) {
        case 'отключённый_таймер':
          console.log('     💡 Действие: Включить таймер актуальности');
          break;
        case 'скорое_истечение':
          console.log('     💡 Действие: Немедленно обновить таймер');
          break;
        case 'предупреждение':
          console.log('     💡 Действие: Запланировать обновление таймера');
          break;
        case 'несоответствие_цены':
          console.log('     💡 Действие: Скорректировать цену товара');
          break;
      }
    });
  });
  
  return критическиеСитуации;
}
```

---

## 2. updateActionTimer

Обновление/активация таймеров актуальности минимальной цены для товаров.

### Интерфейс запроса
```typescript
interface PricesStocksActionTimerUpdateRequest {
  product_ids: string[];          // Массив ID товаров для обновления таймеров
}
```

### Интерфейс ответа
```typescript
// Метод возвращает пустой ответ при успехе
// При ошибке выбрасывается исключение
```

### Примеры использования

#### Базовое обновление таймеров
```typescript
async function обновитьТаймерыТоваров() {
  const товары = ['123456', '234567', '345678', '456789'];
  
  try {
    await api.pricesStocks.updateActionTimer({
      product_ids: товары
    });
    
    console.log(`✅ Таймеры актуальности обновлены для ${товары.length} товаров`);
    console.log(`📋 Обновлённые товары: ${товары.join(', ')}`);
    
  } catch (error: any) {
    console.error('❌ Ошибка обновления таймеров:', error.message);
    
    // Попробуем обновить по одному товару для выявления проблемных
    console.log('🔍 Проверяем товары по отдельности...');
    
    for (const товарId of товары) {
      try {
        await api.pricesStocks.updateActionTimer({
          product_ids: [товарId]
        });
        console.log(`✅ ${товарId}: таймер обновлён`);
        
      } catch (индивидуальнаяОшибка: any) {
        console.error(`❌ ${товарId}: ${индивидуальнаяОшибка.message}`);
      }
      
      // Пауза между запросами
      await new Promise(resolve => setTimeout(resolve, 500));
    }
  }
}
```

#### Автоматическое обновление истекающих таймеров
```typescript
async function автообновлениеИстекающихТаймеров() {
  console.log('🔄 Автоматическое обновление истекающих таймеров');
  
  // Получаем список всех товаров (в реальности это может быть из базы данных)
  const всеТовары = ['123456', '234567', '345678', '456789', '567890'];
  
  // Проверяем статус таймеров
  const статус = await api.pricesStocks.getActionTimerStatus({
    product_ids: всеТовары
  });
  
  if (!статус.statuses) {
    console.log('🔍 Информация о таймерах не получена');
    return;
  }
  
  // Находим товары с истекающими таймерами (в течение 24 часов)
  const истекающиеТаймеры = статус.statuses.filter(товар => {
    if (!товар.timer_expires_at) return false;
    
    const часовДоИстечения = (new Date(товар.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60);
    return часовДоИстечения <= 24 && часовДоИстечения > 0;
  });
  
  if (истекающиеТаймеры.length === 0) {
    console.log('✅ Истекающие таймеры не найдены');
    return;
  }
  
  console.log(`⚠️ Найдено ${истекающиеТаймеры.length} истекающих таймеров`);
  
  // Сортируем по приоритету (сначала те, что истекают раньше)
  истекающиеТаймеры.sort((a, b) => {
    const времяA = new Date(a.timer_expires_at!).getTime();
    const времяB = new Date(b.timer_expires_at!).getTime();
    return времяA - времяB;
  });
  
  // Обновляем таймеры пакетами
  const размерПакета = 10;
  let обновленоУспешно = 0;
  let ошибок = 0;
  
  for (let i = 0; i < истекающиеТаймеры.length; i += размерПакета) {
    const пакет = истекающиеТаймеры.slice(i, i + размерПакета);
    const товарыПакета = пакет.map(т => т.product_id);
    
    try {
      await api.pricesStocks.updateActionTimer({
        product_ids: товарыПакета
      });
      
      обновленоУспешно += товарыПакета.length;
      console.log(`✅ Пакет ${Math.floor(i / размерПакета) + 1}: обновлено ${товарыПакета.length} таймеров`);
      
      // Выводим детали для каждого товара в пакете
      пакет.forEach(товар => {
        const часовДоИстечения = (new Date(товар.timer_expires_at!).getTime() - Date.now()) / (1000 * 60 * 60);
        console.log(`   📦 ${товар.product_id}: истекал через ${Math.round(часовДоИстечения)}ч`);
      });
      
    } catch (error: any) {
      ошибок += товарыПакета.length;
      console.error(`❌ Пакет ${Math.floor(i / размерПакета) + 1}: ошибка обновления`);
      console.error(`   Товары: ${товарыПакета.join(', ')}`);
      console.error(`   Ошибка: ${error.message}`);
    }
    
    // Пауза между пакетами
    if (i + размерПакета < истекающиеТаймеры.length) {
      await new Promise(resolve => setTimeout(resolve, 2000));
    }
  }
  
  console.log(`\n📊 Результат автообновления:`);
  console.log(`   ✅ Успешно обновлено: ${обновленоУспешно} таймеров`);
  console.log(`   ❌ Ошибок: ${ошибок} таймеров`);
  console.log(`   📈 Успешность: ${Math.round(обновленоУспешно / (обновленоУспешно + ошибок) * 100)}%`);
}
```

#### Массовое обновление всех таймеров
```typescript
async function массовоеОбновлениеТаймеров(товарыИзБД: string[]) {
  console.log(`🔄 Массовое обновление таймеров для ${товарыИзБД.length} товаров`);
  
  const размерПакета = 50; // Оптимальный размер для API
  const результаты = {
    обработано: 0,
    успешно: 0,
    ошибок: 0,
    детали: [] as Array<{ пакет: number; товары: string[]; статус: 'успех' | 'ошибка'; ошибка?: string }>
  };
  
  const общееКоличествоПакетов = Math.ceil(товарыИзБД.length / размерПакета);
  
  for (let i = 0; i < товарыИзБД.length; i += размерПакета) {
    const номерПакета = Math.floor(i / размерПакета) + 1;
    const пакет = товарыИзБД.slice(i, i + размерПакета);
    
    console.log(`📦 Обрабатываем пакет ${номерПакета}/${общееКоличествоПакетов} (${пакет.length} товаров)`);
    
    try {
      await api.pricesStocks.updateActionTimer({
        product_ids: пакет
      });
      
      результаты.успешно += пакет.length;
      результаты.детали.push({
        пакет: номерПакета,
        товары: пакет,
        статус: 'успех'
      });
      
      console.log(`   ✅ Успешно обновлено ${пакет.length} таймеров`);
      
    } catch (error: any) {
      результаты.ошибок += пакет.length;
      результаты.детали.push({
        пакет: номерПакета,
        товары: пакет,
        статус: 'ошибка',
        ошибка: error.message
      });
      
      console.error(`   ❌ Ошибка в пакете ${номерПакета}: ${error.message}`);
    }
    
    результаты.обработано += пакет.length;
    
    // Показываем прогресс
    const прогресс = Math.round(результаты.обработано / товарыИзБД.length * 100);
    console.log(`📊 Прогресс: ${прогресс}% (${результаты.обработано}/${товарыИзБД.length})`);
    
    // Пауза между пакетами для соблюдения лимитов API
    if (i + размерПакета < товарыИзБД.length) {
      console.log('   ⏳ Пауза 3 секунды...');
      await new Promise(resolve => setTimeout(resolve, 3000));
    }
  }
  
  // Финальный отчёт
  console.log(`\n🎉 Массовое обновление завершено:`);
  console.log(`   📦 Всего товаров: ${товарыИзБД.length}`);
  console.log(`   ✅ Успешно обновлено: ${результаты.успешно}`);
  console.log(`   ❌ Ошибок: ${результаты.ошибок}`);
  console.log(`   📈 Успешность: ${Math.round(результаты.успешно / товарыИзБД.length * 100)}%`);
  
  // Показываем детали ошибок
  const ошибочныеПакеты = результаты.детали.filter(д => д.статус === 'ошибка');
  if (ошибочныеПакеты.length > 0) {
    console.log(`\n⚠️ Пакеты с ошибками:`);
    ошибочныеПакеты.forEach(пакет => {
      console.log(`   📦 Пакет ${пакет.пакет}: ${пакет.ошибка}`);
      console.log(`      Товары: ${пакет.товары.slice(0, 5).join(', ')}${пакет.товары.length > 5 ? '...' : ''}`);
    });
  }
  
  return результаты;
}
```

---

## 🏗️ Классы для реализации

### Менеджер таймеров
```typescript
interface КонфигурацияТаймеров {
  размерПакета?: number;            // Размер пакета для обработки
  задержкаМеждуПакетами?: number;   // Задержка между пакетами (мс)
  предупреждениеЗаЧасов?: number;   // Предупреждение за сколько часов до истечения
  критическиеЧасы?: number;         // Критический период в часах
  максимумПопыток?: number;         // Максимум попыток при ошибках
}

class МенеджерТаймеров {
  private api: OzonSellerAPI;
  private конфиг: Required<КонфигурацияТаймеров>;
  
  constructor(api: OzonSellerAPI, конфиг: КонфигурацияТаймеров = {}) {
    this.api = api;
    this.конфиг = {
      размерПакета: 50,
      задержкаМеждуПакетами: 2000,
      предупреждениеЗаЧасов: 24,
      критическиеЧасы: 2,
      максимумПопыток: 3,
      ...конфиг
    };
  }

  /**
   * Автоматический мониторинг и обновление таймеров
   */
  async автоматическийМониторинг(товары: string[]): Promise<{
    проверено: number;
    обновлено: number;
    предупреждения: number;
    критические: number;
    ошибки: number;
  }> {
    console.log(`🔄 Запуск автоматического мониторинга для ${товары.length} товаров`);
    
    const результат = {
      проверено: 0,
      обновлено: 0,
      предупреждения: 0,
      критические: 0,
      ошибки: 0
    };
    
    // Получаем статус всех таймеров пакетами
    const всеСтатусы: any[] = [];
    
    for (let i = 0; i < товары.length; i += this.конфиг.размерПакета) {
      const пакет = товары.slice(i, i + this.конфиг.размерПакета);
      
      try {
        const статус = await this.api.pricesStocks.getActionTimerStatus({
          product_ids: пакет
        });
        
        if (статус.statuses) {
          всеСтатусы.push(...статус.statuses);
          результат.проверено += статус.statuses.length;
        }
        
      } catch (error: any) {
        console.error(`❌ Ошибка получения статуса для пакета: ${error.message}`);
        результат.ошибки += пакет.length;
      }
      
      if (i + this.конфиг.размерПакета < товары.length) {
        await new Promise(resolve => setTimeout(resolve, 1000));
      }
    }
    
    // Анализируем статусы и определяем действия
    const требуютОбновления: string[] = [];
    const предупреждения: any[] = [];
    const критические: any[] = [];
    
    всеСтатусы.forEach(товар => {
      if (!товар.is_timer_enabled) {
        требуютОбновления.push(товар.product_id);
        return;
      }
      
      if (товар.timer_expires_at) {
        const часовДоИстечения = (new Date(товар.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60);
        
        if (часовДоИстечения <= this.конфиг.критическиеЧасы) {
          критические.push(товар);
          требуютОбновления.push(товар.product_id);
        } else if (часовДоИстечения <= this.конфиг.предупреждениеЗаЧасов) {
          предупреждения.push(товар);
          требуютОбновления.push(товар.product_id);
        }
      }
    });
    
    результат.предупреждения = предупреждения.length;
    результат.критические = критические.length;
    
    // Выводим информацию о найденных проблемах
    if (критические.length > 0) {
      console.log(`🚨 Критические таймеры (истекают в течение ${this.конфиг.критическиеЧасы}ч): ${критические.length}`);
    }
    
    if (предупреждения.length > 0) {
      console.log(`⚠️ Предупреждения (истекают в течение ${this.конфиг.предупреждениеЗаЧасов}ч): ${предупреждения.length}`);
    }
    
    // Обновляем таймеры при необходимости
    if (требуютОбновления.length > 0) {
      console.log(`🔄 Обновляем ${требуютОбновления.length} таймеров`);
      
      const обновлено = await this.массовоеОбновлениеТаймеров(требуютОбновления);
      результат.обновлено = обновлено.успешно;
      результат.ошибки += обновлено.ошибок;
    }
    
    return результат;
  }

  /**
   * Массовое обновление таймеров с обработкой ошибок
   */
  async массовоеОбновлениеТаймеров(товары: string[]): Promise<{
    успешно: number;
    ошибок: number;
    детали: Array<{ товары: string[]; статус: 'успех' | 'ошибка'; ошибка?: string }>;
  }> {
    const результат = {
      успешно: 0,
      ошибок: 0,
      детали: [] as Array<{ товары: string[]; статус: 'успех' | 'ошибка'; ошибка?: string }>
    };
    
    for (let i = 0; i < товары.length; i += this.конфиг.размерПакета) {
      const пакет = товары.slice(i, i + this.конфиг.размерПакета);
      let успешно = false;
      let попытка = 0;
      let последняяОшибка = '';
      
      // Повторные попытки при ошибках
      while (!успешно && попытка < this.конфиг.максимумПопыток) {
        попытка++;
        
        try {
          await this.api.pricesStocks.updateActionTimer({
            product_ids: пакет
          });
          
          успешно = true;
          результат.успешно += пакет.length;
          результат.детали.push({
            товары: пакет,
            статус: 'успех'
          });
          
        } catch (error: any) {
          последняяОшибка = error.message;
          
          if (попытка < this.конфиг.максимумПопыток) {
            console.log(`⚠️ Попытка ${попытка} неуспешна, повтор через 5 секунд...`);
            await new Promise(resolve => setTimeout(resolve, 5000));
          }
        }
      }
      
      if (!успешно) {
        результат.ошибок += пакет.length;
        результат.детали.push({
          товары: пакет,
          статус: 'ошибка',
          ошибка: последняяОшибка
        });
      }
      
      // Пауза между пакетами
      if (i + this.конфиг.размерПакета < товары.length) {
        await new Promise(resolve => setTimeout(resolve, this.конфиг.задержкаМеждуПакетами));
      }
    }
    
    return результат;
  }

  /**
   * Создание отчёта по таймерам
   */
  async создатьОтчётПоТаймерам(товары: string[]): Promise<{
    общаяСтатистика: {
      всегоТоваров: number;
      активныхТаймеров: number;
      отключённыхТаймеров: number;
      истекающихВ24ч: number;
      истекающихВ2ч: number;
    };
    рекомендации: string[];
    детальныйОтчёт: any[];
  }> {
    const статусы = await this.получитьВсеСтатусы(товары);
    
    const статистика = {
      всегоТоваров: статусы.length,
      активныхТаймеров: 0,
      отключённыхТаймеров: 0,
      истекающихВ24ч: 0,
      истекающихВ2ч: 0
    };
    
    const рекомендации: string[] = [];
    const детальныйОтчёт: any[] = [];
    
    статусы.forEach(товар => {
      const детали = {
        товарId: товар.product_id,
        таймерВключён: товар.is_timer_enabled,
        датаИстечения: товар.timer_expires_at,
        часовДоИстечения: товар.timer_expires_at ? 
          (new Date(товар.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60) : null,
        текущаяЦена: товар.current_price,
        рекомендованнаяЦена: товар.recommended_price,
        последнееОбновлениеЦены: товар.last_price_update
      };
      
      детальныйОтчёт.push(детали);
      
      if (товар.is_timer_enabled) {
        статистика.активныхТаймеров++;
        
        if (товар.timer_expires_at) {
          const часов = (new Date(товар.timer_expires_at).getTime() - Date.now()) / (1000 * 60 * 60);
          
          if (часов <= 2) {
            статистика.истекающихВ2ч++;
          } else if (часов <= 24) {
            статистика.истекающихВ24ч++;
          }
        }
      } else {
        статистика.отключённыхТаймеров++;
      }
    });
    
    // Генерируем рекомендации
    if (статистика.отключённыхТаймеров > 0) {
      рекомендации.push(`Включить таймеры для ${статистика.отключённыхТаймеров} товаров`);
    }
    
    if (статистика.истекающихВ2ч > 0) {
      рекомендации.push(`СРОЧНО обновить ${статистика.истекающихВ2ч} таймеров (истекают в течение 2 часов)`);
    }
    
    if (статистика.истекающихВ24ч > 0) {
      рекомендации.push(`Запланировать обновление ${статистика.истекающихВ24ч} таймеров (истекают в течение 24 часов)`);
    }
    
    const процентАктивных = Math.round(статистика.активныхТаймеров / статистика.всегоТоваров * 100);
    if (процентАктивных < 90) {
      рекомендации.push(`Низкий процент активных таймеров: ${процентАктивных}%. Рекомендуется поддерживать >90%`);
    }
    
    return {
      общаяСтатистика: статистика,
      рекомендации,
      детальныйОтчёт
    };
  }

  private async получитьВсеСтатусы(товары: string[]): Promise<any[]> {
    const всеСтатусы: any[] = [];
    
    for (let i = 0; i < товары.length; i += this.конфиг.размерПакета) {
      const пакет = товары.slice(i, i + this.конфиг.размерПакета);
      
      try {
        const статус = await this.api.pricesStocks.getActionTimerStatus({
          product_ids: пакет
        });
        
        if (статус.statuses) {
          всеСтатусы.push(...статус.statuses);
        }
        
      } catch (error: any) {
        console.error(`Ошибка получения статуса: ${error.message}`);
      }
      
      if (i + this.конфиг.размерПакета < товары.length) {
        await new Promise(resolve => setTimeout(resolve, 1000));
      }
    }
    
    return всеСтатусы;
  }
}
```

---

## ⚠️ Обработка ошибок

### Частые сценарии ошибок
```typescript
async function устойчивыеОперацииСТаймерами() {
  try {
    // Проверка статуса с обработкой ошибок
    const статус = await api.pricesStocks.getActionTimerStatus({
      product_ids: ['НЕСУЩЕСТВУЮЩИЙ_ID']
    });
    
  } catch (error: any) {
    if (error.response?.status === 400) {
      console.error('❌ Неправильные параметры запроса');
      console.log('💡 Проверьте правильность product_ids');
      
    } else if (error.response?.status === 404) {
      console.error('🔍 Товары не найдены');
      console.log('💡 Убедитесь, что товары существуют в системе');
      
    } else {
      console.error('❌ Ошибка получения статуса таймеров:', error.message);
    }
  }
  
  try {
    // Обновление таймеров с обработкой ошибок
    await api.pricesStocks.updateActionTimer({
      product_ids: ['123456']
    });
    
  } catch (error: any) {
    if (error.response?.status === 400) {
      console.error('❌ Неправильные параметры обновления');
      
    } else if (error.response?.status === 429) {
      console.error('⏰ Превышен лимит запросов');
      console.log('💡 Увеличьте интервалы между запросами');
      
    } else if (error.response?.status === 403) {
      console.error('🔒 Недостаточно прав для обновления таймеров');
      
    } else {
      console.error('❌ Ошибка обновления таймеров:', error.message);
    }
  }
}
```

---

## 🔗 Связанная документация

- **[API Цены и Остатки (22-цены-остатки.md)](./22-цены-остатки.md)** - Основной обзор API
- **[Управление ценами (22-цены-остатки-цены.md)](./22-цены-остатки-цены.md)** - Обновление и получение цен
- **[Управление остатками (22-цены-остатки-остатки.md)](./22-цены-остатки-остатки.md)** - Операции с остатками товаров

---

**Статус реализации**: ✅ Завершено  
**Последнее обновление**: 2024  
**Версия API**: v1  
**Количество методов**: 2 метода управления таймерами