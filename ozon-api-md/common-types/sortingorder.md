# SortingOrder

Вид сортировки:
  - `ASC` — по возрастанию,
  - `DESC` — по убыванию.


## Full schema (JSON)
```json
{
  "default": "ASC",
  "enum": null,
  "type": "string",
  "title": "string",
  "description": "Вид сортировки:\n  - `ASC` — по возрастанию,\n  - `DESC` — по убыванию.\n"
}
```
