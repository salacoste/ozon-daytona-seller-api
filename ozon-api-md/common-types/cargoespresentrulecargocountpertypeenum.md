# CargoesPresentRuleCargoCountPerTypeEnum

Тип грузоместа:
- `BOX` — коробка,
- `PALLET` — палета.


## Full schema (JSON)
```json
{
  "type": "string",
  "description": "Тип грузоместа:\n- `BOX` — коробка,\n- `PALLET` — палета.\n",
  "enum": [
    "BOX",
    "PALLET"
  ]
}
```
