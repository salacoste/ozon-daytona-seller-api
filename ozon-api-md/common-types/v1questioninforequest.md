# v1QuestionInfoRequest

## Top-level fields
- `v1QuestionInfoRequest` (top-level fields):
  - `question_id`: `string`

## Full schema (JSON)
```json
{
  "type": "object",
  "title": "object",
  "properties": {
    "question_id": {
      "type": "string",
      "description": "Идентификатор вопроса"
    }
  },
  "required": [
    "question_id"
  ]
}
```
