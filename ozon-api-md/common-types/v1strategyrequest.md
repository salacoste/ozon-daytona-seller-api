# v1StrategyRequest

## Top-level fields
- `v1StrategyRequest` (top-level fields):
  - `strategy_id`: `string`

## Full schema (JSON)
```json
{
  "required": [
    "strategy_id"
  ],
  "type": "object",
  "title": "object",
  "properties": {
    "strategy_id": {
      "type": "string",
      "description": "Идентификатор стратегии."
    }
  }
}
```
