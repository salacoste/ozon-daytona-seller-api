# v1GiveoutInfoRequest

## Top-level fields
- `v1GiveoutInfoRequest` (top-level fields):
  - `giveout_id`: `integer`

## Full schema (JSON)
```json
{
  "required": [
    "giveout_id"
  ],
  "type": "object",
  "properties": {
    "giveout_id": {
      "description": "Идентификатор отгрузки.",
      "type": "integer",
      "format": "int64"
    }
  }
}
```
